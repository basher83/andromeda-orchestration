---
- name: restart netdata
  ansible.builtin.systemd:
    name: netdata
    state: restarted
    daemon_reload: true

- name: reload consul
  ansible.builtin.systemd:
    name: consul
    state: reloaded
